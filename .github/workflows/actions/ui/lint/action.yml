name: UI lint
description: UI lint

inputs:
  rootPath:
    description: Path to project root
    required: true

runs:
  using: 'composite'
  steps:
    - name: Set up base UI
      id: uiSetup
      uses: ./.github/workflows/actions/ui/base-setup

    - uses: ./.github/workflows/actions/ui/install-dependencies
      with:
        rootPath: ${{inputs.rootPath}}
        pnpmKeysPrefix: ${{steps.uiSetup.outputs.pnpmKeysPrefix}}
        pnpmPath: ${{steps.uiSetup.outputs.pnpmPath}}

    - name: Lint
      shell: bash
      run: cd ${{inputs.rootPath}} && pnpm lint:fix
