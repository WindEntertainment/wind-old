name: Engine build
description: Engine build

runs:
  using: 'composite'
  steps:
    - uses: ./.github/workflows/modules/engine/base-setup

    - uses: ./.github/workflows/modules/engine/install-dependencies
