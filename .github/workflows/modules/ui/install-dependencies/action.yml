name: Install dependencies UI
description: Install dependencies UI

runs:
  using: 'composite'
  steps:
    - name: here
      shell: bash
      run: pwd
    - name: Install dependencies
      shell: bash
      run: cd ${{ env.uiRoot }} && pnpm install
