name: Script build
description: Script build

runs:
  using: 'composite'
  steps:
    - uses: ./.github/workflows/modules/scripts/base-setup

    - uses: ./.github/workflows/modules/scripts/install-dependencies

    - name: build
      shell: bash
      run: dotnet ${{ env.buildType }}
