name: Script build
description: Script build

runs:
  using: 'composite'
  steps:
    - uses: ./.github/workflows/utils/setup-dotnet

    - uses: ./.github/workflows/modules/scripts/install-dependencies

    - name: build
      shell: bash
      run: dotnet ${{ env.scriptBuildType }}
